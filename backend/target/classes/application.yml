server:
  port: 8080

spring:
  application:
    name: voltmart-backend

  mail:
    host: smtp.gmail.com
    port: 587
    username: manojcarrer1851@gmail.com
    password: zhxjnmudqsxhmndn

    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  datasource:
    url: jdbc:postgresql://localhost:5433/voltmart
    username: postgres
    password: Manimanoj
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

    database-platform: org.hibernate.dialect.PostgreSQLDialect

  jackson:
    serialization:
      write-dates-as-timestamps: false

app:
  jwt:
    secret: voltmart-super-secret-key-voltmart-super-secret-key
    expiration: 86400000

  cors:
    allowed-origins: http://localhost:3000

  whatsapp:
    support-number: 919398546891
    enabled: false
    api-version: v25.0
    phone-number-id:
    access-token:

  email:
    enabled: true
    from-address: manojcarrer1851@gmail.com
    from-name: VoltMart

logging:
  level:
    org.springframework.security: INFO